version: "2"
sql:
  - engine: "mysql"
    schema: "db/migrations"
    queries: "db/queries"
    gen:
      go:
        package: "mariadb"
        out: "internal/adapter/mariadb/sqlc"
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true
